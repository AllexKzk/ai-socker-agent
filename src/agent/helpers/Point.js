export default class Point {
    constructor(name, x, y, dist, angle, distChange, angleChange) {
        this.name = name
        this.x = x
        this.y = y
        this.distance = dist
        this.angle = angle
        this.distChange = distChange
        this.angleChange = angleChange
    }
}